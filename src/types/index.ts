export type Place = {
  id: string;
  name: string;
  description: string;
  latitude: number;
  longitude: number;
};

export type {
  AppLanguage,
  AuthState,
  OnboardingProfile,
} from './user';
